{
  "name": "vocab-builder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && npm run postbuild",
    "postbuild": "cp src/manifest.json src/study.html src/icon.png dist/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "compromise": "^14.14.4",
    "dexie": "^4.0.9"
  },
  "devDependencies": {
    "vite": "^6.1.1"
  }
}
